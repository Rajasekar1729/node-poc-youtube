    <%- include('../layouts/header.ejs') %>
      <!--Main Navigation-->
    <div class="container">
        <div class="row">
            <h2 class="text-center">User List</h2>
        </div>

        <div class="row">

            <div class="col-md-12">


                <table id="datatable" class="table table-striped table-bordered" cellspacing="0" width="100%">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Age</th>
                            <th>Action</th>
                        </tr>
                    </thead>

                    <tbody>
                        <% if (userList) { %>
                        <% userList.forEach(function(user){ %>
                        <tr>
                            <td><%= user.username %></td>
                            <td><%= user.email %></td>
                            <td><%= user.age %></td>
                            <td>
                                <div style="text-align:center">
                                    <form method="get" action="/users/edit/<%= user._id %>" title="Edit"
                                        style="float: left;">
                                        <button class="btn btn-primary btn-xs" data-title="Edit">
                                            <span class="glyphicon glyphicon-pencil"></span>
                                        </button>
                                    </form>
                                    <form method="post" action="/users/delete/<%= user._id %>" title="Delete"
                                        style="float:left; margin-left: 10px;">
                                        <button type="submit" class="btn btn-danger btn-xs" data-title="Delete"
                                            name="delete" value='Delete'
                                            onClick="return confirm('Are you sure you want to delete?')">
                                            <span class="glyphicon glyphicon-trash"></span>
                                        </button>
                                        <input type="hidden" name="_method" value="DELETE" />
                                    </form>
                                </div>

                            </td>
                        </tr>
                        <% }) %>
                        <% } %>
                    </tbody>
                </table>


            </div>
        </div>
    </div>
    <script>
        $(document).ready(function () {
//             $('#datatable').dataTable({
//                 initComplete: function(){
//                     $("#datatable_length").append('<button type="button" class="btn btn-primary" style="margin-left: 10px;" onClick="redirectToUserAdd();">Create New</button>');

//    } 
//             });

            $('#datatable').dataTable();

        });

        function redirectToUserAdd() {
            window.location.replace('/users/create');
        }
        
    </script>
<%- include('../layouts/footer.ejs') %>